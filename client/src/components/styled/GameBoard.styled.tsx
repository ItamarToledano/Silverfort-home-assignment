import { styled } from "@mui/material/styles";

export const GameBoardStyledContainer = styled("div")({
  display: "flex",
  flexDirection: "column",
  gap: 20,
  marginBottom: 32,
});

export const GameBoardStyledRow = styled("div")({
  display: "flex",
  justifyContent: "center",
  gap: 20,
});
